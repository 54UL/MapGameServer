#ifndef IMAP_COMMAND_H
#define IMAP_COMMAND_H

//Considering rename this to IMapNetObject

namespace MAP{
    class IMapNetPayload{
        public:
        IMapNetPayload();
        virtual ~IMapNetPayload();

        //type As(typecode)
        //Set(typecode,value)

        
    };
}

#endif